{
  "name": "charlie-life-journal",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A private life journal for Unraid",
  "engines": { "node": ">=24.14.0 <25" },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "build": "node --check server.js && node --check public/app.js && node --check public/sections.js"
  }
}
